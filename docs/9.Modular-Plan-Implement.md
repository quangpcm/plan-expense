# Modular Plan Implementation Breakdown

Version: 1.0

Status: Ready for Execution

Last Updated: 2026-08-19

---

# 1. Mục tiêu tài liệu

Tài liệu này chuyển `docs/8.Modular-Plan-Architecture-Specs.md` thành kế hoạch triển khai cụ thể theo codebase hiện tại.

Tài liệu này dùng để:

- chia modular-plan thành phase triển khai an toàn
- map từng phase với code hiện tại
- xác định phần nào refactor trước, phần nào để sau
- giảm rủi ro làm gãy `wedding` flow hiện có
- làm checkpoint cho `travel` và `debt`

Tài liệu này phải được đọc cùng với:

- `docs/8.Modular-Plan-Architecture-Specs.md`
- `docs/2.SDD.md`
- `docs/4.DMS.md`
- `docs/5.Development-Architecture.md`

---

# 2. Phạm vi V1.1

Architecture gate V1.1 chỉ triển khai và kiểm chứng cho:

- `general`
- `wedding`
- `travel`
- `debt`

Các `planType` còn lại:

- `saving`
- `birthday`
- `event`
- `shared_living`
- `project`

chưa phải mục tiêu normalize đầy đủ trong phase này.

Có thể giữ behavior hiện tại cho các plan type đó cho đến khi V1.1 pass.

---

# 3. Quyết định kỹ thuật đã chốt

## 3.1 Milestone compatibility anchor

V1.1 chưa refactor bỏ `milestone` khỏi Finance persistence.

Rule:

- nếu plan bật `planning` module, milestone hoạt động bình thường
- nếu plan không bật `planning` nhưng `finance` vẫn cần milestone reference, app tự tạo đúng 1 hidden milestone
- hidden milestone có `isSystemHidden: boolean`

## 3.2 Hidden milestone lifecycle

- tạo lúc create plan
- mỗi plan không có `planning` chỉ có đúng 1 hidden milestone
- không edit
- không delete
- không reorder
- không hiển thị lên planning UI
- không hiển thị trong selector của user
- expense/income form auto-fill ngầm

## 3.3 Transitional architecture của Finance

Trong V1.1:

- Finance độc lập ở UX/navigation/composition
- Finance chưa độc lập hoàn toàn ở persistence
- `milestoneId` vẫn là compatibility anchor cho schema/service/statistic/aggregate/rules/tests

## 3.4 Phase đầu của Module Registry

Module Registry V1.1 chỉ cần quản lý:

- navigation
- overview widgets
- capability map
- route ownership / collection ownership metadata

Chưa cần generic hóa toàn bộ domain service ngay ở phase đầu.

---

# 4. Hiện trạng codebase

## 4.1 Điểm đã sẵn sàng

- `Plan` đã có `planType`
- category preset đã map theo `planType`
- milestone template đã map theo `planType`
- dữ liệu đang xoay quanh `/plans/{planId}/...`
- `wedding-guest` đã là extension module tương đối tách biệt

## 4.2 Điểm đang coupling

### Plan detail

`src/app/(authenticated)/plans/[planId]/page.tsx`

Hiện đang:

- hard-code tabs
- import trực tiếp nhiều module
- special-case `wedding`
- chưa render theo `PlanTypeConfig + ModuleRegistry`

### Permission

`src/modules/member/services/permission.service.ts`

Hiện đang:

- trả về boolean cố định như `canManageWeddingGuest`
- chưa đi theo capability/module map

### Finance

`src/modules/expense/services/expense.service.ts`

`src/modules/income/services/income.service.ts`

Hiện đang:

- yêu cầu milestone
- assume user sẽ chọn milestone từ UI
- chưa có khái niệm hidden system milestone

### Plan create/delete

`src/modules/plan/repositories/firestore-plan.repository.ts`

Hiện đang:

- tạo milestone template theo `planType`
- chưa có `PlanTypeConfig`
- chưa có hidden milestone flow
- delete plan vẫn hard-code subcollection names

---

# 5. Nguyên tắc triển khai

- không rewrite lớn một lần
- không làm gãy `wedding` behavior hiện tại
- refactor tầng config/composition trước, rồi mới mở extension mới
- chỉ generic hóa chỗ đang là core concern
- giữ domain-specific logic trong module tương ứng
- mọi phase đều phải giữ app chạy được với dữ liệu hiện tại

---

# 6. Tổng quan phase

## Phase 0

Stabilize spec and implementation contract

## Phase 1

Introduce modular-plan shared types and config

## Phase 2

Introduce hidden milestone compatibility layer

## Phase 3

Extract capability-based permission resolver

## Phase 4

Extract Module Registry and PlanType Resolver

## Phase 5

Refactor PlanDetail navigation and tab composition

## Phase 6

Refactor overview composition to widget-based renderer

## Phase 7

Harden data ownership, route ownership, delete-plan cleanup

## Phase 8

Travel validation pass

## Phase 9

Debt prototype and architecture gate

---

# 7. Phase 0 - Stabilize Implementation Contract

## 7.1 Mục tiêu

Đảm bảo spec, DMS và implementation plan không mâu thuẫn trước khi code.

## 7.2 Output chính

- `docs/8.Modular-Plan-Architecture-Specs.md` đã phản ánh hidden milestone strategy
- `docs/9.Modular-Plan-Implement.md` trở thành playbook triển khai
- scope V1.1 được chốt rõ

## 7.3 Task Breakdown

- rà lại DMS để thêm `isSystemHidden` cho milestone
- rà lại Development Architecture để bổ sung `ModuleRegistry` layer
- ghi rõ `debt` không có planning UI nhưng có hidden milestone nội bộ
- xác nhận `travel` vẫn có `planning + travelItinerary + finance + members`

## 7.4 Checklist Pass

- không còn mâu thuẫn giữa spec và implementation strategy
- hidden milestone contract xuất hiện ở đúng các tài liệu liên quan

---

# 8. Phase 1 - Shared Types and PlanType Config

## 8.1 Mục tiêu

Tạo tầng config/types cho modular-plan mà chưa đụng mạnh vào UI lớn.

## 8.2 Output chính

- shared types cho module/config/capability
- `PlanTypeConfig` resolver
- `ModuleDefinition` registry skeleton

## 8.3 Task Breakdown

### Shared types

Tạo module mới, ví dụ:

- `src/modules/plan-config/types`
- hoặc `src/modules/plan/runtime/types`

Định nghĩa:

- `PlanModuleId`
- `PlanCapability`
- `PlanModuleDefinition`
- `PlanModuleConfig`
- `PlanTypeConfig`
- `OverviewWidgetDefinition`
- `OverviewWidgetConfig`

### Plan type config

Tạo config cho:

- `general`
- `wedding`
- `travel`
- `debt`

Mỗi config cần chốt:

- enabled modules
- module order
- label override
- overview widget list

### Resolver

Tạo utility:

- `getPlanTypeConfig(planType)`
- `getEnabledModules(plan)`
- `hasModule(plan, moduleId)`

## 8.4 File/Area dự kiến bị ảnh hưởng

- `src/modules/plan/types/plan.ts`
- module config mới trong `src/modules/plan` hoặc `src/modules/plan-config`

## 8.5 Checklist Pass

- resolve được module list từ `planType`
- chưa cần sửa `PlanDetail`
- wedding/travel/debt config đã có contract rõ

---

# 9. Phase 2 - Hidden Milestone Compatibility Layer

## 9.1 Mục tiêu

Giữ nguyên compatibility của Finance hiện tại nhưng hỗ trợ plan không có planning UI.

## 9.2 Output chính

- milestone có `isSystemHidden`
- create plan flow tạo hidden milestone khi cần
- expense/income auto-resolve hidden milestone
- planning UI không lộ hidden milestone

## 9.3 Task Breakdown

### Data model

Update milestone schema/document:

- thêm `isSystemHidden: boolean`

Update DMS và repository mapping tương ứng.

### Plan create flow

`src/modules/plan/repositories/firestore-plan.repository.ts`

Refactor `createPlanGraph()`:

- không chỉ dựa vào `planType` cũ
- resolve module config trước
- nếu `planning` enabled:
  - tạo milestone templates bình thường
- nếu `planning` disabled và `finance` enabled:
  - tạo đúng 1 hidden milestone

### Runtime resolver

Tạo helper:

- `resolveDefaultMilestoneForPlan(planId, milestones, config)`

Rule:

- nếu planning enabled thì dùng flow hiện tại
- nếu planning disabled thì resolve hidden milestone

### Expense / Income service

`src/modules/expense/services/expense.service.ts`

`src/modules/income/services/income.service.ts`

Refactor:

- cho phép UI không truyền milestone selector khi planning disabled
- service tự gán hidden milestone
- vẫn giữ `milestoneId` bắt buộc ở persistence layer

### UI filtering

Filter hidden milestone khỏi:

- milestone list
- milestone search
- milestone selector
- planning statistic UI

Không filter hidden milestone khỏi:

- internal aggregation
- finance totals cấp plan

## 9.4 File/Area dự kiến bị ảnh hưởng

- `src/modules/milestone/types/milestone.ts`
- `src/modules/milestone/repositories/firestore-milestone.repository.ts`
- `src/modules/plan/repositories/firestore-plan.repository.ts`
- `src/modules/expense/services/expense.service.ts`
- `src/modules/income/services/income.service.ts`
- `src/modules/expense/components/expense-form.tsx`
- `src/modules/income/components/income-form.tsx`
- `src/modules/statistic/services/statistic.service.ts`

## 9.5 Checklist Pass

- debt/general style plan có thể chạy finance mà không lộ planning UI
- wedding/travel vẫn hoạt động như cũ
- statistic tổng không sai
- hidden milestone không hiện lên UI

---

# 10. Phase 3 - Capability-based Permission Layer

## 10.1 Mục tiêu

Tách permission khỏi feature-specific boolean để chuẩn bị cho module registry.

## 10.2 Output chính

- capability map tập trung
- resolver trả được capability set
- generic UI kiểm capability/module thay vì plan type

## 10.3 Task Breakdown

### Permission model

Giữ role hiện tại:

- `owner`
- `editor`
- `viewer`

Nhưng thêm capability resolver:

- `planning.createMilestone`
- `planning.editTodo`
- `finance.createExpense`
- `finance.createIncome`
- `finance.manageSettlements`
- `members.manage`
- `weddingGuests.manageGuest`
- `travelItinerary.createActivity`
- `debtTracking.createDebt`

### Compatibility layer

Giai đoạn đầu có thể giữ `ResolvedPlanPermissions`, nhưng nên:

- build nó từ capability resolver
- tránh thêm boolean mới kiểu `canManageDebt`

### Service migration

Refactor dần các service:

- `wedding-guest`
- `expense`
- `income`
- `settlement`
- `member`

để dùng capability resolver.

## 10.4 File/Area dự kiến bị ảnh hưởng

- `src/modules/member/services/permission.service.ts`
- `src/modules/member/types/member.ts`
- service của `expense`, `income`, `settlement`, `wedding-guest`

## 10.5 Checklist Pass

- permission không còn gắn chặt vào `wedding`
- thêm module mới không yêu cầu thêm nhiều boolean hard-code trong core

---

# 11. Phase 4 - Module Registry and Ownership Metadata

## 11.1 Mục tiêu

Đưa metadata của module về một nơi duy nhất để generic layer dùng được.

## 11.2 Output chính

- `moduleRegistry`
- route ownership metadata
- collection ownership metadata
- capability ownership metadata

## 11.3 Task Breakdown

### Module registry

Mỗi module định nghĩa:

- `id`
- `defaultLabel`
- `navigation.enabled`
- `permissions`
- `overviewWidgets`
- `routes`
- `collections`

### Ownership metadata

Mỗi module khai báo ownership của:

- routes
- subcollections
- widgets
- capabilities

Ví dụ:

- `planning` owns `milestones`, `todos`
- `finance` owns `expenses`, `incomes`, `settlements`
- `weddingGuests` owns `weddingGuests`, `weddingGuestGroups`, `guestInvitations`
- `travelItinerary` owns `travelActivities`
- `debtTracking` owns `debts`, `repayments`

### Delete-plan preparation

Tạo helper để list subcollections theo enabled module hoặc ownership registry.

Chưa cần thay delete flow ngay nếu còn rủi ro.

## 11.4 Checklist Pass

- registry resolve được module metadata
- ownership không còn phải hard-code rải rác

---

# 12. Phase 5 - PlanDetail Navigation and Tab Composition

## 12.1 Mục tiêu

Tách `PlanDetail` khỏi hard-coded tab list và plan-type checks.

## 12.2 Output chính

- tab/navigation sinh từ config
- module screen map
- `PlanDetail` generic hơn

## 12.3 Task Breakdown

### Navigation model

Refactor:

`src/app/(authenticated)/plans/[planId]/page.tsx`

Hiện trạng:

- hard-coded `tabs`
- `TAB_BY_QUERY_PARAM`
- special-case `plan.planType === 'wedding'`

Mục tiêu:

- derive visible modules từ `PlanTypeConfig`
- map module sang tab item
- map query param sang module/tab

### Screen composition

Tách render block theo module:

- overview section
- planning section
- finance section
- members section
- wedding guest section

Giai đoạn đầu chưa cần chia nhỏ toàn bộ page thành nhiều route con.

Có thể dùng pattern:

- `moduleRenderers`
- `tabDefinitions`
- `moduleScreenMap`

### Backward-safe strategy

Không rewrite toàn file `PlanDetail` một lần.

Nên làm theo bước:

1. tạo derived navigation model
2. thay phần tab header bằng model mới
3. thay từng block render theo module id
4. giữ logic business cũ đến khi flow pass

## 12.4 Checklist Pass

- wedding không cần special-case tab
- travel có thể hiện `planning + itinerary + finance + members`
- debt có thể hiện `overview + debtTracking + finance`

---

# 13. Phase 6 - Widget-based Overview

## 13.1 Mục tiêu

Biến overview thành composition layer thay vì hard-code theo wedding-first layout.

## 13.2 Output chính

- overview widget registry
- overview renderer
- config-driven widget ordering
- `OverviewTab` chỉ còn là shell gọi renderer

## 13.3 Task Breakdown

### Widget extraction

V1.1 không cần viết lại widget domain từ đầu.

Ưu tiên:

- reuse component hiện có
- tách composition khỏi `OverviewTab`
- để `OverviewRenderer` chịu trách nhiệm resolve order/filter/inject props

Phân loại widget hiện có:

- summary cards
- upcoming milestone/todo
- finance overview
- member summary
- wedding guest summary

### Widget registry

Mỗi widget cần:

- `widgetId`
- `moduleId`
- component reference
- input contract

V1.1 cho phép:

- widget component reuse component/domain section hiện có
- widget mới chỉ cần mỏng, không cần rich UI riêng

Hai widget extension cần có thật ngay từ phase đầu:

- `travelItinerarySummary`
- `debtSummary`

Nhưng chỉ cần ở mức summary widget mỏng.

### Renderer

Tạo:

- `OverviewRenderer`
- `resolveOverviewWidgets(plan)`

Rule:

- `OverviewRenderer` lọc widget trước hoàn toàn
- chỉ render widget khi module đang enabled và đủ data contract
- widget không tự fallback `null`

### OverviewTab shell

`overview-tab.tsx` nên được refactor thành shell mỏng:

- nhận overview props tổng
- render outer spacing / shell concern nếu cần
- gọi `OverviewRenderer`

Không giữ logic composition trực tiếp trong `OverviewTab`.

### Migration strategy

Wedding overview là baseline đầu tiên.

Travel overview phase đầu có thể reuse phần lớn widget của wedding/core, chỉ thay order/composition.

Debt overview tối thiểu ở V1.1:

- plan summary
- debt summary
- finance summary

## 13.4 Checklist Pass

- thay đổi widget order không phải sửa `PlanDetail`
- disable module không làm overview crash
- `OverviewTab` không còn chứa business composition lớn
- `travelItinerarySummary` và `debtSummary` tồn tại như widget riêng thật

---

# 14. Phase 7 - Data Ownership and Cleanup Hardening

## 14.1 Mục tiêu

Giảm hard-code data ownership, chuẩn bị cho extension mới.

## 14.2 Output chính

- delete-plan cleanup bám ownership metadata
- create/watch/query flow rõ module ownership
- rules/tests có checklist cho hidden milestone
- hidden milestone bị chặn mọi client write sau bootstrap create

## 14.3 Task Breakdown

### Delete plan cleanup

Refactor:

`src/modules/plan/repositories/firestore-plan.repository.ts`

Thay vì hard-code subcollections, chuyển dần sang:

- registry-backed collection list
- hoặc helper list collections theo module ownership

### Rules/Test checklist

Rà các vùng:

- `firestore.rules`
- `tests/rules/firestore.rules.test.ts`

Bổ sung nguyên tắc:

- `isSystemHidden = true` không được phép bị update/delete/reorder từ mọi client flow, kể cả owner
- ngoại lệ duy nhất là bootstrap create trong cùng flow tạo plan
- giữ nguyên current collection structure ở phase này, không đổi shape của `repayments`

Bổ sung cases:

- hidden milestone không editable từ user flow
- hidden milestone không deletable
- finance create/update vẫn pass khi planning UI disabled

### Aggregate guard

Kiểm tra:

- milestone totals
- plan totals
- hidden milestone filtered khỏi planning UI

Phase 7 chỉ harden tới mức A:

- ưu tiên `deletePlan`
- ưu tiên rules/tests
- không quét mọi hard-code ownership còn lại trong cùng phase

Nên thêm một guard/test để nếu module mới khai báo collection trong registry mà ownership helper không trả ra đúng danh sách cleanup, test sẽ fail.

## 14.4 Checklist Pass

- thêm module collection mới không làm delete-plan quên cleanup
- hidden milestone không tạo security hole
- rules chặn được hidden milestone write trực tiếp sau bootstrap create

---

# 15. Phase 8 - Travel Validation Pass

## 15.1 Mục tiêu

Kiểm chứng `travel` thật sự reuse core modules và thêm extension đúng thiết kế.

## 15.2 Output chính

- `travel` config hoạt động
- planning và itinerary tách biệt
- finance không phụ thuộc itinerary

## 15.3 Task Breakdown

### Travel core validation

Đảm bảo `travel` dùng:

- `planning`
- `finance`
- `members`

không duplicate domain riêng kiểu:

- `travelMilestone`
- `travelTodo`
- `travelExpense`

### Travel itinerary prototype

Tạo `travelItinerary` extension với:

- `travelActivities`
- optional link `Expense.activityId`
- planned cost tách khỏi actual expense

`Expense.activityId` ở phase đầu chỉ là liên kết nhẹ để itinerary có thể tham chiếu expense nếu cần.

- expense vẫn là record finance độc lập
- xóa activity không được xóa expense
- nếu activity bị xóa thì `expense.activityId` phải được set về `null`

### UI validation

Travel phải có:

- planning tab
- itinerary tab
- finance tab
- members tab

## 15.4 Checklist Pass

- itinerary disable không làm finance crash
- activity delete không xóa expense và clear `expense.activityId`
- travel không special-case trong generic plan detail

## 15.5 Decision Chốt Cho Phase Này

- không làm flow bind sâu giữa expense form và travel activity ở phase đầu
- `travelItinerarySummary` giữ ở mức widget mỏng, chỉ đọc aggregate/shared props đã được chuẩn bị sẵn
- ưu tiên validation mức runtime + build + manual verification, chưa bắt buộc test suite riêng cho itinerary-expense link trong phase này

---

# 16. Phase 9 - Debt Prototype and Architecture Gate

## 16.1 Mục tiêu

Dùng `debt` làm bài kiểm tra cuối cho modular-plan V1.1.

## 16.2 Output chính

- `debt` config hoạt động
- debt tracking là extension riêng
- finance vẫn dùng được qua hidden milestone

## 16.3 Task Breakdown

### Debt module prototype

Tạo extension:

- `debts`
- `repayments`

Debt không cần planning tab.

Debt có:

- overview
- debt tracking
- finance

Ở V1.1, debt được siết về đúng 1 use case:

- mỗi khoản debt là quan hệ giữa `tôi` và đúng `1 member` khác trong plan
- không hỗ trợ debt giữa hai member bất kỳ
- không hỗ trợ actor ngoài plan ở phase này

Semantics finance đi kèm:

- `repayment` là domain event riêng của `debtTracking`, không tự sync sang finance
- nếu tôi cho người khác mượn tiền, tôi tự tạo `expense`
- nếu người khác trả lại cho tôi, tôi tự tạo `income`
- `outstandingAmount` chỉ tính `principal - totalRepayment`

Scope V1.1 giữ mỏng:

- không tính lãi/phí
- chỉ có status `active | paid`
- chưa làm edit/delete cho debt hoặc repayment
- debt overview gồm `planSummary + debtSummary + financeSummary`
- `debtSummary` nên dẫn user sang tab `debtTracking`

### Hidden milestone validation

Đảm bảo:

- plan create tự sinh hidden milestone
- expense/income không lộ milestone selector
- statistic planning dimension không lộ hidden milestone

### Architecture gate review

Checklist cuối:

- wedding không gãy
- travel reuse core
- debt thêm domain mới mà không sửa generic core quá nhiều

## 16.4 Checklist Pass

- debt hoạt động mà không cần planning UI
- modular registry/config điều khiển được plan detail
- hidden milestone strategy pass thực tế
- debt UX phản ánh đúng mô hình `tôi <-> 1 member`
- wording/summary trong overview và debt tracking không còn mang ngữ nghĩa generic loan system

## 16.5 Status Review Cuối

### Done

- `DONE` `debt` config hoạt động qua `plan-type-config`, `module registry`, `PlanDetail` shell và tab composition
- `DONE` debt có đủ `overview + debtTracking + finance`, không cần planning tab
- `DONE` `debtTracking` là extension riêng với `debts` + `repayments`
- `DONE` finance của debt vẫn chạy qua hidden milestone nội bộ
- `DONE` hidden milestone không lộ lên planning UI, milestone list, selector và debt finance UX
- `DONE` expense/income form của debt không buộc user thao tác với milestone
- `DONE` `repayment` là domain event riêng, không tự sync sang finance
- `DONE` debt semantics đã được siết về mô hình `tôi` và `1 member` còn lại trong plan
- `DONE` `outstandingAmount` chỉ tính `principal - totalRepayment`
- `DONE` debt status giữ mỏng ở mức `active | paid`
- `DONE` `debtSummary` đã xuất hiện trong overview và có CTA mở sang tab `debtTracking`
- `DONE` wording/category presets/empty states của debt finance đã được chỉnh sát domain cá nhân hơn
- `DONE` modular shell hiện tại đã chứa debt mà không cần phá generic core quá nhiều
- `DONE` `npm run build` pass sau toàn bộ thay đổi Phase 9

### Partial

- `PARTIAL` `wedding không gãy`
  Hiện đã qua build và modular shell chung vẫn hoạt động, nhưng chưa có regression test hoặc checklist manual riêng cho toàn bộ wedding flows sau các thay đổi Phase 9.
- `PARTIAL` `travel reuse core`
  Travel đã chạy trên cùng modular pattern và không có special-case lớn mới phát sinh, nhưng chưa có một vòng verify/manual pass đầy đủ sau khi debt hoàn tất.
- `PARTIAL` `hidden milestone strategy pass thực tế`
  Về code path/UI/runtime là đã pass, nhưng chưa có thêm round test/rules riêng mới cho debt phase ngoài các hardening đã làm ở phase trước.

### Chưa Done

- `NOT DONE` edit/delete cho `debt` và `repayment`
  Đây là chủ đích scope, không phải bug.
- `NOT DONE` sync tự động giữa `repayment` và finance transactions
  Theo chốt nghiệp vụ, phase này cố ý không làm.
- `NOT DONE` hỗ trợ debt giữa hai member bất kỳ hoặc actor ngoài plan
  Phase này đã chốt không hỗ trợ.
- `NOT DONE` rules/tests chuyên biệt cho toàn bộ debt flows
  Hiện chưa có bộ verify chuyên sâu riêng cho debt như một phase test độc lập.

---

# 17. Thứ tự file nên sửa

Thứ tự khuyến nghị để giảm rủi ro:

1. docs/spec liên quan
2. shared modular types/config
3. milestone data model + hidden milestone support
4. create plan flow
5. expense/income hidden milestone resolution
6. permission capability resolver
7. module registry
8. plan detail navigation
9. overview renderer
10. delete-plan cleanup + rules/tests
11. travel extension
12. debt extension

---

# 18. Không nên làm trong V1.1

- rewrite toàn bộ `PlanDetail` thành nested routes ngay từ đầu
- generic hóa sâu mọi service/domain chỉ vì muốn “đẹp”
- normalize tất cả `planType` hiện có ngoài 4 loại gate
- bỏ milestone khỏi persistence layer
- làm full Debt product trước khi modular core pass
- mở rộng Travel sang booking/route optimization

---

# 19. Definition of Pass cho tài liệu này

Implementation breakdown này được coi là usable khi:

- mỗi phase có mục tiêu, output, task breakdown, checklist rõ
- team có thể code theo thứ tự mà không phải suy diễn lại strategy
- mọi quyết định lớn đều bám `docs/8.Modular-Plan-Architecture-Specs.md`
- không còn mơ hồ giữa “target architecture” và “transitional V1.1 implementation”

---

# 20. Next Step đề xuất

Sau tài liệu này, thứ tự thực thi khuyến nghị là:

1. cập nhật DMS cho `isSystemHidden`
2. tạo shared modular types/config
3. implement hidden milestone flow ở `createPlanGraph()`
4. refactor expense/income service để auto-resolve hidden milestone
5. tạo module registry và refactor navigation của `PlanDetail`

Khi 5 bước này pass, mới nên đi tiếp sang `travelItinerary` và `debtTracking`.
