# Pre-Go-Live Visual Polish — Report

## Files changed

- `src/modules/plan/constants/plan-card-config.ts` (Finding 1)
- `src/modules/member/components/member-management-panel.tsx` (Finding 2)
- `src/modules/member/components/members-tab.tsx` (Finding 2)
- `src/modules/plan/constants/overview-widget-registry.wedding.tsx` (Finding 3A)
- `src/modules/plan/constants/overview-widget-registry.tsx` (Finding 3B)
- `src/modules/invitation/components/invitation-list.tsx` (Finding 4)

Exactly the 6 files listed in the manifest's Strict Boundary. No other source file was touched.

## Finding 1 — Dashboard money format: DONE

`getSafeCurrency()` in `plan-card-config.ts` now calls `formatCompactCurrency` instead of
`formatCurrency`. One function, one file — `formatCurrency`/`formatCompactCurrency` themselves,
`plan-card.tsx`, `build-plan-card-view-model.ts`, and money masking logic are all untouched.
Verified: `315.602.000 ₫` → `315,6 triệu` (no ellipsis), a second plan type (Travel,
`87.654.321 ₫` → `87,7 triệu`), the ended-plan summary path, the budget-progress ratio label
(`315,6 triệu / 500 triệu`), the locked/masked case (still renders `•••••••` — masking wraps the
string and never inspects content, unaffected), and a small value (500₫, correctly falls back to
`formatCompactCurrency`'s own <1,000 branch, i.e. exact `formatCurrency` output, not compacted to
nothing).

## Finding 2 — Members terminology: DONE

All 8 copy strings from the manifest updated in `member-management-panel.tsx` (7) and
`members-tab.tsx` (1). "Mời thành viên" / "Mời người khác tham gia bằng email hoặc liên kết." were
already correct and left untouched. The `memberType: 'guest'` unlink-account description in
`member-list.tsx` was **not** touched, per instruction. No identifier (`guestForm`, `guestMessage`,
`AddGuestSchema`, `memberService.addGuest`, etc.) was renamed — only string literal values changed.
Zero references under `src/modules/wedding-guest/` were touched.

## Finding 3 — Overview density: DONE (both changes)

**A. Wedding `AttentionItemRow` (desktop):** replaced `lg:rounded-[26px] lg:border ... lg:shadow-...
lg:hover:-translate-y-0.5 ... lg:hover:shadow-...` with `lg:rounded-[20px] lg:border
lg:border-slate-200 lg:bg-white lg:hover:border-slate-300 lg:hover:bg-slate-50` — no independent
drop shadow or hover-lift, canonical 20px radius (matching the "Sắp tới" `DataRow` rows two
paragraphs below in the same widget). Mobile's `divide-y`/no-border base classes are untouched (the
new classes are all `lg:`-prefixed). No content, urgency logic, click handler, todo data, or the
surrounding `Card`/`Section` was touched.

**B. Travel `TravelItinerarySummaryWidget`:** removed the
`rounded-[24px] border border-slate-200 bg-slate-50/70 p-4` box around "Tổng lịch trình." Same
label/count/supporting-copy now render as plain right-aligned typography
(`text-lg font-semibold text-slate-700` for the number, down from `text-3xl font-semibold
text-slate-950`), deliberately lower visual weight than the left column's activity title
(`text-xl font-semibold`). The 2-column grid structure and all travel data/logic are unchanged.

## Finding 4 — Pending invitation density: DONE

`InvitationList` now renders each invitation as a `DataRow` (`main` = identity + role, `trailing` =
status `Badge` + conditional "Hủy lời mời" `Button`, stacked vertically) instead of a full `Card`
per item, wrapped in `grid gap-2` (was `grid gap-3` of full Cards). `DataRow` has no `onClick` — it
stays non-interactive, exactly per instruction; the explicit "Hủy lời mời" button remains the only
action. `canRevoke`/`invitation.status === 'pending'` gating and `onRevoke(invitation)` are
byte-identical to before. **Long-identity guardrail:** the identity paragraph uses `break-words`
(not `truncate`) — a long email wraps onto a second line rather than ever being clipped, verified
with a deliberately long test address rendering in full. `DataRow`'s public API was not touched.
The zero-invitation empty `<Card>` state is unchanged.

## Business / permission / data / calculation changes: NO

- No service, repository, schema, or type file was touched in any of the four findings.
- Finding 1 changes only how an already-computed number is *displayed*; the numbers themselves
  (progress `value`/`max`, `totalExpense`, `budgetAmount`, etc.) are untouched — confirmed no
  calculation file was modified.
- Finding 2 is string-literal-only; every handler, service call (`memberService.addGuest`,
  `invitationService.createInvitation`), and validation schema is unchanged.
- Finding 3 touches only Tailwind classNames and a stat's presentation weight — no prop, data
  source, or business logic in either widget changed.
- Finding 4 preserves `canRevoke`, `invitation.status === 'pending'`, and `onRevoke` exactly;
  `DataRow` itself (a Design System component) was not modified.

## Automated verification

- `tsc --noEmit`: clean.
- `npm run lint`: 0 issues in all 6 changed files. Repo-wide: 21 problems (4 errors / 17 warnings)
  — confirmed identical to the pre-existing baseline reported at every prior Wave (unrelated files:
  `member-management-panel.tsx`'s pre-existing React Hook Form warning, unused-import warnings in
  `overview-widget-registry.tsx`/`.wedding.tsx`/`plan-card-config.ts` that predate this batch,
  `use-attention-todos.ts`/`use-todos-by-milestone.ts` effect-setState errors, `travel-activity`
  unused imports).
- `npm test`: 170/170 passed (27 files).
- `npm run build` (webpack, production): compiled clean, typechecked clean, all 13 routes
  generated.
- `next-env.d.ts`/`tsconfig.tsbuildinfo` reverted after each run; final diff is exactly the 6 files
  listed in §1.
- One incident during this pass, unrelated to the code changes: the long-running local dev server
  became unresponsive (spinning at high CPU) after a production build ran against the same `.next`
  directory it had open — restarted cleanly (`rm -rf .next` + fresh `npm run dev`) before targeted
  visual verification; noted here for transparency, not a defect in any changed file.

## Targeted visual verification

Isolated temp route (`src/app/visual-polish-verify-temp/page.tsx`, mock data only, no Firestore,
deleted after use) mounting `PlanCard`, `MemberManagementPanel`, `InvitationList`, and the two
specific widget components directly from `overviewWidgetRegistry`, driven by Playwright at desktop
(1280×1400) and mobile (390×1600):

```
                                                        desktop   mobile
Dashboard — no "…" ellipsis on any PlanCard value         PASS      PASS
Dashboard — Wedding + Travel (2nd PlanType) both correct   PASS      PASS
Dashboard — locked/masked shows ••••••• (unaffected)       PASS      PASS
Dashboard — ended-plan + progress-ratio label compact      PASS      PASS
Members — "Thêm thành viên"/"Mời thành viên" present       PASS      PASS
Members — stray "Thêm khách" absent                        PASS      PASS
Invitations — 1 + 6 rows render, "Hủy lời mời" ×7          PASS      PASS
Invitations — long email preserved in full (no clipping)   PASS      PASS
Zero nested <button> anywhere on the page                  PASS      PASS
Wedding AttentionItemRow — new lg: classes present,        PASS      PASS
  no shadow/26px/hover-lift remnants
Travel itinerary stat — plain "text-right", no border/bg   PASS      PASS
```

One hydration warning was observed in the harness console: caused by the temp route's own mock
`joinedAt: Timestamp.now()` being evaluated at physically different moments during SSR vs. client
hydration, which feeds `formatRelativeTime` (pre-existing, untouched code in `buildUpdatedFooter`)
a different relative-time string each time ("bây giờ" vs. "N giây trước"). This is a mock-data
artifact of the verification harness, not a regression — confirmed via diff that
`formatRelativeTime`/`buildUpdatedFooter`/`joinedAt` handling were not touched by any of the four
findings, and in production `joinedAt` is a frozen Firestore timestamp, never re-evaluated between
render passes.

Temp route and `.next` cache removed after verification; final diff contains only the 6 files
listed in §1.

## Remaining visual blockers

None.

## Final disposition

**VISUAL POLISH PASS**

All four approved findings are implemented exactly as scoped, verified with targeted (non-
exhaustive) checks at one desktop and one mobile width, confirmed NO across business/permission/
data/calculation impact, and pass the full automated gate (typecheck/lint/170 tests/build) against
the same pre-existing lint baseline as every prior wave.
