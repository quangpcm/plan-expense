# Wave 3 Report — travel-activity

**Risk tier:** R2 · **Disposition:** APPROVE (no code changes required)

## 1. Files changed

None. Bounded inventory found zero legacy-overlay candidates in scope, so no migration was
performed this wave.

## 2. Bounded inventory (actual counts)

Scope: `src/modules/travel-activity` (12 files: `travel-activity-detail.tsx`,
`travel-activity-form.tsx`, `travel-activity-list.tsx`, `travel-itinerary-tab.tsx`, plus
hooks/services/schemas/types/utils). Checked **one directory per `grep` call**, per the lesson
recorded in the Wave 2 report (a joined multi-path invocation silently drops matches with this
repo's `grep` wrapper).

- `BottomSheet`: **0** real usages — the only hit was a code comment in
  `travel-activity-detail.tsx` ("nằm trong BottomSheet/Dialog") describing layout context, not an
  actual import/usage.
- raw `Dialog`/`AlertDialog` import: **0**.
- `window.confirm`: **0** — the one flow that existed (delete travel activity) was already
  migrated to `ConfirmDialog` in Wave 0, at the page level.
- `Button size="icon"`: **0**.
- Consumers of `TravelActivityForm`/`TravelActivityDetail`/`TravelItineraryTab`/
  `TravelActivityList` app-wide: only `(authenticated)/plans/[planId]/page.tsx` (already cleaned in
  Wave 0) — no other entry point.
- Selection UI in `travel-activity-form.tsx` already uses the V2 `DropdownSelect` component (2
  usages) — no legacy custom picker (unlike `ExpenseForm`'s payer-picker `BottomSheet` found in
  Wave 2) to migrate.

## 3. MIGRATE / KEEP / DEFER summary

| Item | Disposition | Reason |
|---|---|---|
| Ad-hoc `rounded-[24px]`/`rounded-[28px]` divs (`travel-activity-detail.tsx` ×2, `travel-itinerary-tab.tsx` ×3 incl. 2 `Skeleton`s) | KEEP / no action | Not `Card` component instances — standalone containers. No V2 token matches 24px/28px exactly (`--radius-ds-lg` 16px, `--radius-ds-xl` 20px); per the Wave 1 exact-alias principle, no accurate 1:1 mapping exists to force. Same disposition as Wave 2. |
| `Card` usages (3 files) | KEEP | Already the V2 `Card` component at its intentional legacy-compat default (Wave 4 decision) — nothing to migrate. |

No MIGRATE candidates. No DEFER candidates.

## 4. V2 components adopted

None new this wave.

## 5. Business / permission / data changes

**NO.** No files were touched.

## 6. Automated verification

Not re-run — no source file was changed in this wave, so there is nothing new for
typecheck/lint/tests/build to catch beyond what Wave 2's run already confirmed clean on the
now-unchanged tree. Re-running the full suite against a zero-diff wave would not exercise anything
this wave actually did.

## 7. Targeted P0/P1 verification

Not applicable — no behavior was changed.

## 8. Wave 3 guardrail compliance

Per this wave's explicit guardrail, travel-activity's product-specific timeline/activity semantics
(chronological ordering, upcoming/next-activity behavior, category semantics, location/map
behavior) were not touched, examined for "improvement," or genericized — the bounded inventory
found no overlay-legacy surface to migrate in the first place, so this guardrail was never in
tension with any candidate change.

## 9. Findings / deferred debt

None new. No Design System backlog evidence surfaced this wave (`Button size="icon"`, `Progress`,
`Plan-Type Expression`, `TextAction`, `NotificationBadge`, bottom-panel — none encountered).

## 10. Final disposition

**APPROVE.** Wave 3 complete, zero code changes — the module was already clean by the time its
turn came up (Wave 0's page-level cleanup had already removed its one legacy item, the
`window.confirm` on delete).

**STOP per instruction — Manual QA Checkpoint #1.** Waves 1–3 are done:
- Wave 1 (storage/user/auth/invitation, R1): `AttachmentPicker`'s `BottomSheet` → `ResponsiveModal`.
- Wave 2 (expense/milestone/planning/income, R2): `ExpenseForm`'s payer-picker `BottomSheet` →
  `ResponsiveModal`; `PlanningTab`'s drilldown `BottomSheet` confirmed KEEP (policy-exempt).
- Wave 3 (travel-activity, R2): no changes needed.

Net diff across all three waves: 2 files (`attachment-picker.tsx`, `expense-form.tsx`), 2
`BottomSheet` → `ResponsiveModal` migrations, 0 business/permission/data changes, 170/170 tests
green, clean production build after each wave. Wave 4 (member) is **not** started — awaiting your
manual QA pass.
