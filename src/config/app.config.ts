export const appConfig = {
  name: 'Plan Expense',
  description: 'Mini expense sharing web app for plans, trips, and group funds.',
  defaultLocale: 'en',
  defaultCurrency: 'VND',
  defaultTimezone: 'Asia/Ho_Chi_Minh',
} as const;

