export const appRoutes = {
  home: '/',
  login: '/login',
  register: '/register',
  forgotPassword: '/forgot-password',
  plans: '/plans',
  profile: '/profile',
  settings: '/settings',
} as const;
